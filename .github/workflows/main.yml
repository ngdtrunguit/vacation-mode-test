on:
  issues:

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Update Vacation Mode
    steps:
    - name: vacation mode action step
      uses: robotnyc/vacation-mode-action@v1
      with:
        limit-group: 'collaborators_only'
